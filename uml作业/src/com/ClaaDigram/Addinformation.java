package com.ClaaDigram;

import java.util.List;

public interface Addinformation {

   Teacher addTeacher(Teacher teacher);
   Student addStudent(Student student);
   Administrator addAdminsirator(Administrator administrator);
   Teacher  changeTeacher(Teacher teacher);
   Student  changeStudent(Student student);
   Administrator changeAdministrator();

}
